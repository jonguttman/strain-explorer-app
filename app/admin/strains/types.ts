// Admin types - re-exporting from lib/types.ts for convenience
export type {
  DoseKey,
  TraitAxisId,
  VisualConfig,
  DoseSnapshot,
  StrainMeta,
  StrainTestimonials,
  StrainJsonEntry,
  DoseConfig,
  DoseConfigMap,
  EditorDataset,
} from "@/lib/types";
